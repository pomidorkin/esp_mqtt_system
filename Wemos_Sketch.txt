#include <ESP8266WiFi.h>
#include <PubSubClient.h>
#include <ArduinoJson.h>

// ========== КОНФИГУРАЦИЯ WEMOS D1 MINI ==========
const char* ssid = "Galaxy A51B860";
const char* password = "gfifgfif";
const char* mqtt_server = "10.164.154.6";
const int mqtt_port = 1883;

// Пины Wemos D1 Mini (отличаются от NodeMCU!)
const int LED_PIN = LED_BUILTIN;  // D4 (GPIO2) - встроенный LED
const int BUTTON_PIN = D3;        // GPIO0
const int SENSOR_PIN = A0;        // Аналоговый вход

// ========== АВТОМАТИЧЕСКИЙ ID ==========
String device_id;
String device_type = "sensor";
String base_topic;

WiFiClient espClient;
PubSubClient client(espClient);

// ========== УНИКАЛЬНАЯ ГЕНЕРАЦИЯ ID ==========
String generateDeviceID() {
  String mac = WiFi.macAddress();
  mac.replace(":", "");
  String id = "WEMOS_" + mac;  // Префикс WEMOS вместо ESP
  return id;
}

void createTopics() {
  base_topic = "devices/" + device_id;
  // ... остальное без изменений
}

// ========== НАСТРОЙКА WIFI ==========
void setup_wifi() {
  delay(10);
  Serial.println();
  Serial.print("📶 Подключение к ");
  Serial.println(ssid);

  WiFi.mode(WIFI_STA);
  WiFi.begin(ssid, password);

  int attempts = 0;
  while (WiFi.status() != WL_CONNECTED && attempts < 30) {
    delay(500);
    Serial.print(".");
    attempts++;
    digitalWrite(LED_PIN, !digitalRead(LED_PIN)); // Мигаем LED
  }

  if (WiFi.status() == WL_CONNECTED) {
    Serial.println("");
    Serial.println("✅ WiFi подключен");
    Serial.print("📡 IP адрес: ");
    Serial.println(WiFi.localIP());
    Serial.print("🔑 MAC адрес: ");
    Serial.println(WiFi.macAddress());
    digitalWrite(LED_PIN, HIGH); // Wemos: HIGH = LED OFF
    return true;
  } else {
    Serial.println("❌ Ошибка подключения к WiFi");
    return false;
  }
}

// ========== MQTT CALLBACK ==========
void callback(char* topic, byte* payload, unsigned int length) {
  // ... без изменений
}

// ========== ПОДКЛЮЧЕНИЕ MQTT ==========
void reconnect() {
  // ... без изменений
}

// ========== ОТПРАВКА СТАТУСА ==========
void sendStatus() {
  DynamicJsonDocument doc(1024);
  
  doc["device_id"] = device_id;
  doc["type"] = device_type;
  doc["ip"] = WiFi.localIP().toString();
  doc["mac"] = WiFi.macAddress();
  doc["rssi"] = WiFi.RSSI();
  doc["free_heap"] = ESP.getFreeHeap();
  doc["uptime"] = millis();
  doc["board"] = "Wemos D1 Mini";  // Добавляем информацию о плате
  doc["version"] = "3.0";
  
  String jsonString;
  serializeJson(doc, jsonString);
  
  if (client.connected()) {
    client.publish(status_topic.c_str(), jsonString.c_str());
    Serial.println("📤 Статус отправлен");
  }
}

// ========== SETUP ==========
void setup() {
  pinMode(LED_PIN, OUTPUT);
  digitalWrite(LED_PIN, HIGH); // Wemos: HIGH = LED OFF
  
  Serial.begin(115200);
  delay(1000);
  
  Serial.println();
  Serial.println("🚀 WEMOS D1 MINI MQTT Client v3.0");
  Serial.println("=================================");
  
  // Инициализация WiFi
  if (!setup_wifi()) {
    Serial.println("🔁 Перезагрузка из-за проблем с WiFi...");
    delay(3000);
    ESP.restart();
  }
  
  // Генерация ID и топиков
  device_id = generateDeviceID();
  createTopics();
  
  Serial.println();
  Serial.println("🎯 КОНФИГУРАЦИЯ WEMOS:");
  Serial.println("   ID: " + device_id);
  Serial.println("   MQTT: " + String(mqtt_server) + ":" + String(mqtt_port));
  Serial.println("   Базовый топик: " + base_topic);
  Serial.println();
  
  // Настройка MQTT
  client.setServer(mqtt_server, mqtt_port);
  client.setCallback(callback);
  client.setBufferSize(2048);
  
  delay(2000);
  Serial.println("✅ Wemos D1 Mini готов к работе");
}

// ========== MAIN LOOP ==========
void loop() {
  if (!client.connected()) {
    reconnect();
  }
  client.loop();

  // Отправка статуса каждые 30 сек
  static unsigned long lastStatus = 0;
  if (millis() - lastStatus > 30000) {
    lastStatus = millis();
    sendStatus();
    
    Serial.print("💾 Heap: ");
    Serial.print(ESP.getFreeHeap());
    Serial.print(" | 📶 RSSI: ");
    Serial.println(WiFi.RSSI());
  }
  
  // Мигание LED при подключении (инверсия для Wemos)
  static unsigned long lastBlink = 0;
  if (client.connected() && millis() - lastBlink > 1000) {
    lastBlink = millis();
    digitalWrite(LED_PIN, !digitalRead(LED_PIN)); // Инверсная логика
  }
  
  delay(100);
}